---
fixtures:
  symlinks:
    sssd: "#{source_dir}"
    auditd: "../#{auditd}"
    augeasproviders_core: "../#{augeasproviders_core}"
    augeasproviders_grub: "../#{augeasproviders_grub}"
    common: "../#{common}"
    concat: "../#{concat}"
    nscd: "../#{nscd}"
    openldap: "../#{openldap}"
    pki: "../#{pki}"
    stdlib: "../#{stdlib}"
